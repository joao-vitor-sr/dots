require('lsp_signature').setup({
	floating_window = true,
	hint_prefix = '',
	hint_enable = false,
	handler_opts = {
		border = 'shadow',   -- double, rounded, single, shadow, none
	},
	padding = ' ',
})
